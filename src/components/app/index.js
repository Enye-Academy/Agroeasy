import * as components from './components';
import * as signinActions from './actions';
import reducers from './reducers';
import * as selectors from './selectors';

export default { components, reducers, selectors, signinActions };
